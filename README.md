# Chat API — тестовое задание
## Асинхронный REST API для управления чатами и сообщениями.

### Реализовано на FastAPI, с использованием SQLAlchemy ORM, поддержкой SQLite для локальной разработки и PostgreSQL в Docker.

### 🚀 Возможности
- Создание чатов
- Отправка сообщений в чат
- Получение чата с последними N сообщениями
- Удаление чата вместе со всеми сообщениями (каскадно)
- Валидация входных данных
- Асинхронная работа с БД
- Миграции через Alembic
- Docker + docker-compose
- Логирование в файл

### 🧱 Стек
- Python 3.11+
- FastAPI
- SQLAlchemy 2.0 (async)
- PostgreSQL
- SQLite (локально)
- Alembic
- Docker / docker-compose

### Запуск
#### 1) Docker
- Перейти в директорию с файлом docker-compose.yml
```docker
cd C:/hitalent_testovoe
```
- Создать файл .env скопировав содержимое с файла env.example
- Запустить файл docker-compose.yml
```docker
docker-compose up -d
```
- После загрузки проект доступен по адресу localhost:8000/docs
#### 2) Локально
- Перейти в директорию hitalent_testovoe
```bash
cd C:/hitalent_testovoe
```
- Создать виртуальное окружение, установить зависимости
```bash
python -m venv .venv
```
- Установить зависимости
```bash
pip install -r src/requirements.txt
```
- Создать файл .env скопировав содержимое с файла env.example
- Переименовать строку DB_ENGINE=postgres на DB_ENGINE=sqlite
- Закомментировать строку DATABASE_URI
- Запустить проект
```bash
uvicorn src.main:app
```
- После загрузки проект доступен по адресу 127.0.0.1:8000/docs
